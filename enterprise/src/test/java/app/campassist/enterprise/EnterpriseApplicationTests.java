package app.campassist.enterprise;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for {@link EnterpriseApplication}.
 *
 * <p>This class is annotated with {@link SpringBootTest} to load the full
 * Spring application context for integration testing.</p>
 *
 * <p>Contains a basic test method {@link #contextLoads()} to ensure that the
 * application context loads successfully.</p>
 */
@SpringBootTest
class EnterpriseApplicationTests {

    /**
     * Test that the Spring application context loads successfully.
     */
    @Test
    void contextLoads() {
    }
}
